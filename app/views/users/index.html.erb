<h1>Users</h1>

<p class="actions">
  <%= link_to "Add user", new_user_path %>
</p>

<table>
  <% @users.each do |user| %>
  <tr>
    <td><%= link_to user.name, user %></td>
    <td><%= user.email %></td>
  </tr>
  <% end %>
</table>
